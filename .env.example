# Required for API key auth. Set a strong random value and use the SAME
# value in Cognigy's HTTP Request Node as the X-API-Key header.
# If unset, the API runs with auth disabled (fine for local dev only -
# NEVER deploy to Netlify without setting this).
API_KEY=changeme-generate-a-real-secret